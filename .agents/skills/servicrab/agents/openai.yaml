interface:
  display_name: "Servicrab"
  short_description: "Repo-specific patterns and workflows for servicrab"
  default_prompt: "Use the servicrab repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true